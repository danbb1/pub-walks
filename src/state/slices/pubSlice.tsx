import { createSlice } from '@reduxjs/toolkit';

export const pubsSlice = createSlice({
  name: 'pubs',
  initialState: null,
  reducers: {},
});

export const {} = pubsSlice.actions;

export default pubsSlice.reducer;
